name: CI/CD for Spring Boot

on:
  push:
    branches: [ main, master ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      # 1. 检出代码
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. 设置 Java 环境
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven  # 添加 Maven 缓存，加速构建

      # 3. 编译项目（跳过测试，避免测试失败导致部署中断）
      - name: Build project
        run: mvn clean package -DskipTests

      # 4. 上传文件到服务器
      - name: Upload to server
        uses: burnett01/rsync-deployments@v8
        with:
          switches: -avz
          path: docker-compose.yml Dockerfile target/*.jar
          remote_path: /data/project/knowseek/backend/
          remote_host: ${{ secrets.SERVER_HOST }}
          remote_user: ${{ secrets.SERVER_USER }}
          remote_key: ${{ secrets.SSH_PRIVATE_KEY }}
          remote_port: ${{ secrets.SERVER_PORT }}

      # 5. 在服务器上构建镜像并部署
      - name: Deploy on server
        uses: appleboy/ssh-action@v1.0.3  # 更新到最新版本
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASS }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            cd /data/project/knowseek/backend/
            
            echo "开始构建新镜像..."
            
            # --remove-orphans 清理未在 compose 文件中定义的孤儿容器
            docker-compose up -d --build --remove-orphans
            
            # 清理虚悬镜像 (dangling images)，释放磁盘空间
            docker image prune -f
            
            echo "等待服务启动..."
            sleep 15
            
            # 检查状态
            docker-compose ps
            
            # 打印最后一点日志确认没有报错
            docker-compose logs --tail=50
            
            echo "🎉 部署完成！"